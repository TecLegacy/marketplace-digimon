import { LucideProps } from 'lucide-react';

const Icons = {
  logo: (props: LucideProps) => (
    // <svg className='' {...props}>
    //   MAKE YOUR OWN SVG LOGO
    // </svg>
    <div>MAKE YOUR OWN SVG LOGO</div>
  ),
};

export default Icons;
